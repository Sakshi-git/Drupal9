/**
 * @file
 * Behaviors for the var_subtheme theme.
 */

(function ($, _, Drupal) {
  Drupal.behaviors.var_subtheme = {
    attach: function () {
      // var_subtheme JavaScript behaviors goes here.
    }
  };
})(window.jQuery, window._, window.Drupal);
